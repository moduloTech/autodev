# frozen_string_literal: true

# Phase A: Rails serves no HTTP traffic yet. Routes will land here in phase B
# (cf. autodev/docs/autospec.md §D — Rails monte Sinatra en middleware).
Rails.application.routes.draw do
end
